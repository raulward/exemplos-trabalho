#include <stdlib.h>
#include <stdio.h>

int main() {

    int num;

    scanf("%d", &num); // Le o numero digitado e o atribui ao endereço de memoria de num
    printf("%d\n", num); // Imprime o valor lido de num

    return 0;
}