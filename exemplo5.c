#include <limits.h>
#include <stdio.h>

int main() {

    printf("O valor máximo limite para o tipo int: %d\n", INT_MAX); // Imprime o maximo para int suportado
    printf("O valor minímo limite para o tipo int: %d\n", INT_MIN); // Imprime o minimo para int suportado

    return 0;
}